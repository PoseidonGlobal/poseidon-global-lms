# Copy to .env.local and set values
NEXTAUTH_SECRET=replace-with-a-long-random-string
# When running locally, Next sets this automatically, but you can override it:
# NEXTAUTH_URL=http://localhost:3000
